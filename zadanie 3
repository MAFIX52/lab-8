#include <iostream>
using namespace std;
int main()
{
	setlocale(LC_ALL, "Russian");
	int s[6] = { 2, 3, 4, 5,7,10 };
	int num = 1;

	for (int i = 0; i < 6; i++) 
	{
		num *= s[i];
	}
	cout << "произведение всех элементов массива - " << num << endl;


	return 0;
	
}
